import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF0F0F0F);
  static const surface = Color(0xFF1C1C1E);
  static const surfaceElevated = Color(0xFF2C2C2E);
  static const surfaceOverlay = Color(0xFF3A3A3C);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8E8E93);
  static const textTertiary = Color(0xFF636366);

  // Semantic
  static const positive = Color(0xFF34C759);    // green, success, CTA
  static const negative = Color(0xFFFF3B30);    // red, error, delete
  static const warning = Color(0xFFFF9F0A);     // yellow, low credits
  static const accent = Color(0xFF0A84FF);       // blue, links, secondary

  // Trades
  static const plumbing = Color(0xFF0A84FF);
  static const electrical = Color(0xFFFF9F0A);
  static const roofing = Color(0xFF34C759);
  static const construction = Color(0xFFBF5AF2);

  // Borders
  static const divider = Color(0xFF38383A);
  static const borderDefault = Color(0xFF48484A);
  static const borderActive = Color(0xFF34C759);

  // Transparent
  static const transparent = Color(0x00000000);

  // Auth screen
  static const loadingOverlay = Color(0x8A000000);
  static const appleSignInBackground = Color(0xFFFFFFFF);
  static const appleSignInForeground = Color(0xFF000000);
}
